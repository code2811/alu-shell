#!/usr/bin/env bash
# Script to list files and display only the part after the first dash (if present)

for file in *; do
    if [[ -f "$file" && "$file" != .* ]]; then
        # Extract the part of the filename after the first dash
        echo "${file#*-}"
    fi
done

